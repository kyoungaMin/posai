# 개발일지 - 2026-03-12 (목)

## 오늘의 목표
- [x] 프로젝트 폴더 구조 생성 (Folder.md 기반)
- [x] pos-insight-ai 모놀리식 소스 → Next.js App Router 구조 전환
- [x] Supabase DB 스키마 설계 및 마이그레이션 작성
- [x] ChatGPT 참조 스키마 비교 분석 및 고도화
- [x] Supabase SSR 인증 및 데이터 레이어 구축
- [x] 대시보드 최적화 (병렬 쿼리, 차트 수정, 이번달 매출)
- [x] 재고관리 전면 개편 (간편 입출고, 안전재고, AI 추천)
- [x] Gemini 모델 업그레이드 (2.0-flash → 2.5-flash)
- [x] AI 매출 예측 TTS 기능 추가
- [x] 날씨 예보 연동 (OpenWeatherMap)

---

## 작업 내역

### 1. 프로젝트 초기 설정
**카테고리:** `인프라`
**상태:** `완료`

**내용:**
- `docs/Folder.md` 기반으로 전체 폴더 구조 생성
- Next.js 15 + React 19 + TypeScript 프로젝트 구성
- Tailwind CSS 커스텀 테마 설정 (primary: #FF6B35, Pretendard 폰트)
- ESLint, PostCSS, tsconfig 설정

**변경 파일:**
- `package.json` - Next.js 15, React 19, Recharts, framer-motion, @google/genai, @supabase/supabase-js
- `next.config.ts` - 기본 Next.js 설정
- `tsconfig.json` - `@/*` → `./src/*` path alias
- `tailwind.config.ts` - 커스텀 색상, Pretendard, 확장 borderRadius
- `postcss.config.js` - Tailwind + autoprefixer
- `.env.local.example` - 환경변수 템플릿
- `.gitignore` - Next.js 표준

### 2. 소스 코드 마이그레이션 (모놀리식 → Next.js App Router)
**카테고리:** `리팩토링`
**상태:** `완료`

**내용:**
- `pos-insight-ai/src/App.tsx` (1495줄) 분해 → 30+ 컴포넌트/페이지/훅
- Express 서버 (`server.ts`) → Next.js API Routes 전환
- 클라이언트 Gemini 호출 → 서버 전용 API Route로 이동 (API 키 보호)
- 탭 네비게이션 → 파일 기반 라우팅 (Sidebar + BottomNav)
- Phase 1 MVP용 인메모리 Mock 데이터 구현

**주요 구조:**
```
src/
├── app/
│   ├── (auth)/login, signup     # 인증 페이지
│   ├── dashboard/               # 대시보드 + 분석
│   ├── inventory/               # 재고 관리
│   ├── marketing/               # SNS 마케팅
│   ├── chat/                    # AI 채팅
│   └── api/                     # API Routes
│       ├── pos/                 # dashboard, analytics, inventory
│       ├── ai/                  # insight, chat, marketing, predict
│       └── social/              # facebook
├── components/
│   ├── ui/                      # StatCard, Modal
│   ├── layout/                  # Sidebar, BottomNav, Header
│   ├── dashboard/               # SalesChart, AIPrediction, RankList, InventoryStatus
│   ├── inventory/               # InventoryTable, AddItemModal
│   └── chat/                    # ChatBubble, MessageInput
├── hooks/                       # useAuth, usePOSData
├── lib/                         # supabase/client, gemini/client, mock-data, utils
└── types/                       # database.ts, pos.ts, ai.ts
```

**변경 파일:** 30+ 파일 생성 (상세 목록은 위 구조 참조)

### 3. Supabase DB 스키마 설계 (Phase 1)
**카테고리:** `설계`
**상태:** `완료`

**내용:**
- PostgreSQL 10개 테이블 설계: stores, profiles, menus, sales, inventory, purchase_orders, daily_summary, social_accounts, marketing_posts, chat_sessions/messages
- RLS(Row Level Security) 정책으로 멀티테넌트 데이터 격리
- `inventory.status` GENERATED COLUMN으로 자동 상태 계산
- 데모용 시드 데이터: 대박카페, 메뉴 6종, 재고 8종, 30일 매출 생성

**변경 파일:**
- `supabase/migrations/001_initial_schema.sql` - 10개 테이블 DDL
- `supabase/migrations/002_rls_policies.sql` - RLS 정책
- `supabase/migrations/003_seed_data.sql` - 시드 데이터 (PL/pgSQL)
- `docs/database.md` - DB 설계 문서 v1.0

### 4. DB 스키마 고도화 (ChatGPT 참조 스키마 비교 분석)
**카테고리:** `설계`
**상태:** `완료`

**내용:**
- `chtgpt_ref.md` (엔터프라이즈급 SaaS DB) vs 우리 스키마 비교 분석
- 7가지 핵심 개선사항 도출 및 반영:
  1. **주문 정규화** - sales_orders + sales_order_items (1:N)
  2. **환불 관리** - refunds 테이블 분리
  3. **레시피 BOM** - recipes_bom으로 메뉴↔재고 매핑
  4. **재고 이력** - inventory_transactions 트랜잭션 로그
  5. **AI 예측 이력** - ai_forecasts 정확도 추적
  6. **알림 시스템** - alerts + alert_settings
  7. **분석 마트** - mart_hourly_sales 시간대별 집계
- TypeScript 타입 정의 동기화
- Phase 계획 3단계 → 5단계 확장

**변경 파일:**
- `supabase/migrations/004_enhanced_schema.sql` - 고도화 테이블 9개 + RLS
- `supabase/migrations/005_seed_bom.sql` - 레시피 BOM 시드 데이터
- `src/types/database.ts` - 고도화 타입 추가
- `docs/database.md` - v2.0 업데이트 (ERD, 테이블 상세, 인덱스, Phase)

### 5. 레거시 소스 삭제
**카테고리:** `기타`
**상태:** `완료`

**내용:**
- `pos-insight-ai/` 폴더 전체 삭제 (마이그레이션 완료 후)

### 6. Supabase SSR 인증 및 데이터 레이어 구축
**카테고리:** `기능개발`
**상태:** `완료`

**내용:**
- Supabase SSR (`@supabase/ssr`) 패키지 도입
- 서버/클라이언트 Supabase 클라이언트 분리 (`server.ts`, `client.ts`)
- Next.js 미들웨어로 보호 라우트 구현 (`middleware.ts`)
- `queries.ts`에 Supabase 쿼리 함수 집약 (getDashboardData, getInventory, getStoreContext 등)
- 로그인/회원가입 페이지 Supabase Auth 연동

**변경 파일:**
- `src/lib/supabase/server.ts` - 서버 전용 Supabase 클라이언트 (쿠키 기반)
- `src/lib/supabase/middleware.ts` - 세션 갱신 미들웨어
- `src/lib/supabase/queries.ts` - 중앙 쿼리 레이어 (15+ 함수)
- `src/middleware.ts` - 보호 라우트 미들웨어
- `src/hooks/useAuth.ts` - Supabase Auth 연동
- `src/app/(auth)/login/page.tsx` - 실제 로그인 동작
- `src/app/(auth)/signup/page.tsx` - 실제 회원가입 동작

### 7. 대시보드 최적화
**카테고리:** `버그수정` `리팩토링`
**상태:** `완료`

**내용:**
- 대시보드 쿼리 5개 순차 실행 → `Promise.all` 병렬화 (로딩 속도 대폭 개선)
- "누적매출" → "이번달 매출"로 변경 (월별 필터 적용)
- 매출 추이 차트 바가 안 보이던 버그 수정 (flex 높이 계산 문제)
- 차트 max 값 동적 계산으로 변경 (하드코딩 1,500,000 제거)

**변경 파일:**
- `src/lib/supabase/queries.ts` - getDashboardData 병렬화, 이번달 필터
- `src/app/dashboard/page.tsx` - "이번달 매출" 라벨 변경
- `src/components/dashboard/SalesChart.tsx` - 바 렌더링 수정, 동적 max

### 8. 재고관리 전면 개편 (간편 입출고 방식)
**카테고리:** `기능개발`
**상태:** `완료`

**내용:**
- 기존 +1/-1 버튼 → 입고/출고 모달 방식으로 전면 개편
- 입출고 시 수량, 거래유형(매입/반품/조정/폐기/판매), 메모 입력 가능
- `inventory_transactions` 테이블에 모든 입출고 이력 기록
- 최근 30건 입출고 이력 표시 (TransactionHistory 컴포넌트)
- 안전재고 인라인 편집 기능 (클릭 → 입력 → Enter/Esc)
- AI 안전재고 추천 기능 (Gemini가 소비 패턴 분석 후 추천)
- 사이드바 메뉴명 "재고 및 발주" → "재고 관리"로 단순화

**변경 파일:**
- `src/app/inventory/page.tsx` - 전면 재작성 (입출고/추천/이력)
- `src/components/inventory/StockModal.tsx` - 신규 (입고/출고 모달)
- `src/components/inventory/TransactionHistory.tsx` - 신규 (입출고 이력)
- `src/components/inventory/InventoryTable.tsx` - 입고/출고 버튼, 안전재고 편집
- `src/components/inventory/AddItemModal.tsx` - 안전재고 필드 추가
- `src/app/api/pos/inventory/route.ts` - stock_in/out, update_safety_stock, recommend 액션
- `src/lib/supabase/queries.ts` - getInventoryTransactions, updateSafetyStock 추가
- `src/components/layout/Sidebar.tsx` - 메뉴명 변경

### 9. Gemini 모델 업그레이드
**카테고리:** `버그수정`
**상태:** `완료`

**내용:**
- `gemini-2.0-flash` 모델 지원 종료(404 에러) → `gemini-2.5-flash`로 전체 교체
- 매출 예측, 채팅, 주간 예보, 마케팅, 범용 텍스트 생성 모두 적용

**변경 파일:**
- `src/lib/gemini/client.ts` - 5개 메서드 모델명 교체
- `src/lib/supabase/queries.ts` - saveAIForecast model_version 변경

### 10. AI 매출 예측 TTS(음성 듣기) 기능
**카테고리:** `기능개발`
**상태:** `완료`

**내용:**
- Web Speech API (`speechSynthesis`) 활용하여 AI 예측 결과를 한국어 음성으로 읽어주는 기능 추가
- 듣기/멈추기 토글 버튼
- "다시 분석" 시 음성 자동 중지

**변경 파일:**
- `src/components/dashboard/AIPrediction.tsx` - TTS 기능 추가

### 11. 날씨 예보 연동
**카테고리:** `기능개발`
**상태:** `완료`

**내용:**
- OpenWeatherMap API 연동으로 실제 날씨 예보 데이터 활용
- 날씨 데이터 DB 캐싱 (weather_forecasts 테이블)
- 주간 매출 예측 시 실제 날씨 반영

**변경 파일:**
- `src/lib/weather/client.ts` - 신규 (OpenWeatherMap 클라이언트)
- `src/app/api/weather/route.ts` - 신규 (날씨 API 라우트)
- `supabase/migrations/006_weather_forecasts.sql` - 날씨 캐시 테이블

---

## 이슈 / 블로커
| 이슈 | 심각도 | 상태 | 해결 방법 |
|------|--------|------|-----------|
| Write 도구 "File has not been read yet" 오류 | 낮음 | 해결 | Read 후 Write, 또는 rm 후 새로 Write |
| Port 3000 EADDRINUSE | 낮음 | 해결 | `netstat` + `taskkill`로 기존 프로세스 종료 |
| `gemini-2.0-flash` 404 에러 (모델 지원 종료) | 높음 | 해결 | `gemini-2.5-flash`로 전체 교체 |
| webpack `Cannot read properties of undefined (reading 'call')` | 중간 | 해결 | `.next` 캐시 삭제 후 재시작 |
| AI 안전재고 추천 500 에러 | 중간 | 해결 | Gemini 응답의 마크다운 코드펜스 제거 처리 추가 |

## 내일 할 일
- [ ] 화면별 점검 계속 (분석, 마케팅, 채팅 페이지)
- [ ] AI 채팅 이력 저장 (chat_sessions, chat_messages)
- [ ] 랜딩 페이지 디자인 보완
- [ ] 마케팅 페이지 Facebook OAuth 연동 검토

## 메모
- Supabase SSR 인증 + 미들웨어로 보호 라우트 완성
- 대시보드 병렬 쿼리로 체감 로딩 속도 크게 개선
- 재고관리는 트랜잭션 기반으로 전환하여 이력 추적 가능
- Gemini API 키는 서버 사이드(API Routes)에서만 사용 → 클라이언트 노출 방지
- 데이터 흐름: Raw → Standard → Mart → AI Output (엔터프라이즈 패턴 채택)
